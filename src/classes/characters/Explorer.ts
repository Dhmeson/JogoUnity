import { Character } from '../Character';
export class Explorer extends Character {
  constructor() {
    super('Explorer', 80, 4, 15, 3, 70, 80, 70);
  }

  // Add methods or properties specific to Explorer if needed
}
