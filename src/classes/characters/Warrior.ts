import { Character } from '../Character';

export class Warrior extends Character {
  constructor() {
    super('Warrior', 120, 3, 20, 10, 60, 120, 60);
  }

  // Add methods or properties specific to Warrior if needed
}
